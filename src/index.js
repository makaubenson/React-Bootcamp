import ReactDOM from "react-dom/client";

import "./index.css";
import App from "./App";
//App is indeed a component: the main component

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
